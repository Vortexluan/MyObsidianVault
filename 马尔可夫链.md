## 基本知识
你爷爷生了你爹，但是你的出生只和你爹有关系.
马尔可夫链就是描述的这样一种随机过程，即下一状态由且仅由上一状态决定，在时间之前的序列状态均与此无关。
如果我们希望给出更确切的定义的话......
1.state space（状态空间）
2.memorylessness（无记忆性）
3.transition matrix（转移矩阵）

我们举一个例子：倘若我今日早上吃的是A小汉堡，那么我有60%的概率在第二天去吃B小米辣，40%的概率在第二天仍然吃A小汉堡。倘若我今天早上吃的是B小米辣，那么我有50%的概率在第二天吃A小汉堡，50%的概率在第二天吃B小米辣。

假设第一天我吃了A小汉堡，那么第二天我所吃的东西概率为（这个东西也被叫做状态向量）
$$
\begin{bmatrix}
P(A) \\
P(B) \\
\end{bmatrix}
$$
左边的矩阵就是转移矩阵，显然它会是一个方阵。
$$
\begin{bmatrix}
0.4 & 0.6 \\
0.5 & 0.5 \\
\end{bmatrix}
\times
\begin{bmatrix}
1 \\
0 \\
\end{bmatrix}
=
\begin{bmatrix}
0.4 \\
0.6 \\
\end{bmatrix}
$$

而倘若是第n天对应的概率，它就会趋近于

$$
\
\begin{bmatrix}
0.4 & 0.6 \\
0.5 & 0.5 \\
\end{bmatrix}
^n
\times
\begin{bmatrix}
1 \\
0 \\
\end{bmatrix}
=
\begin{bmatrix}
0.454545 \\
0.545454 \\
\end{bmatrix}
$$
同样我们可以发现，如果起始条件是P(A)=0,P(B)=1,在n天后我们得到的概率是相同的（当然，并不是所有马尔可夫链都可以得出这个结论，也就是说，马尔可夫链不一定有稳态）

## 稳态条件（平稳分布）
先上一个有点不说人话的结论：
如果一个马尔可夫链的全部状态节点满足常返性，非周期性和任意两状态连通，那么该马尔可夫链便符合遍历性的要求，具有唯一的稳态分布。
1.常返性，若从状态A离开，那必须要有办法返回到状态A。
2.非周期性，若能够从A出发，在过程中经过任意数量的状态再回到A都可行，我们称之为非周期性。
3.两两连通,任意两个状态都有办法到达。



上面叽里咕噜说些什么呢，我们从比较质朴的角度来理解这个问题，想要趋向于稳态，本质上是在n趋近于无穷的时候，有
$$
v_{\infty}=A v_{\infty}
$$
很惊讶的发现这和线性代数又有点关系了，就是说对于一个矩阵，如果它有唯一一个特征值为1的右特征向量，我们可以断言这个马尔可夫链是有稳态解的。此时问题就转化为使得转移矩阵有唯一一个特征值为1的右特征向量呢？
经常学线性代数的朋友们都知道，这种求解全靠试，书上没有给出完全精准的判断，这里难度过高，就不再赘述了。

## 极限分布
就是在n趋近于正无穷的时候的极限状态，在特定条件下，极限分布就是平稳分布。