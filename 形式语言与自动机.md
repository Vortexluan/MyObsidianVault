简单地介绍一下这门课的每一个知识点

克林（Keleene）闭包和正闭包

乔姆斯基文法体系  
0型文法-短语结构文法-图灵机  产生式想怎么写就怎么写
1型文法-上下文有关文法-线性有界自动机 产生式左部可以涉及上下文
2型文法-上下文无关文法-下推自动机 产生式的左部只能有一个非终结符
3型文法-正则文法-有限自动机 产生式的右部只能有一边有非终结符

DFA NFA $\varepsilon$-NFA 正则表达式 这四个东西应当是能够相互转换的
$\varepsilon$-NFA转换为DFA的方法（NFA转DFA同理）
正则语言转化为$\varepsilon$-NFA的方法（Thompson）
$\varepsilon$-NFA转化为NFA的方法：感觉会了，原先的q保持不变，但是在转移的时候是对它的\varepsilon-closure闭包进行转移，这样\varepsilon就不会出现了

化简DFA

将DFA转化为正则表达式 
方法1递归  
$$R_{i,j}^{(k)} = R_{i,j}^{(k-1)}+R_{i,k}^{k-1}(R_{k,k}^{(k-1)})^*R_{k,j}^{(k-1)}$$
方法2状态消去法
事实上应该还有一个 阿登引理

如果有人问DFA NFA $\varepsilon$-NFA的形式化定义/多元组/数学描述
$$A=(Q,\Sigma,\delta,q_0,F)$$
注意 \sum 和\sigma 不能认为是一个东西
对于这三者来讲唯一的区别就是$\delta$的实际定义
DFA $Q \times \Sigma \rightarrow Q$ （**千万要注意写一个构造DFA的题目的时候，每一个状态的每一个转移的线都不能够省略**）
NFA $Q \times \Sigma \rightarrow 2^Q$
$\varepsilon$-NFA $Q \times (\Sigma \cup \{ \varepsilon \} ) \rightarrow 2^Q$


文法
$$G=(V,\Sigma,P,S)$$


文法的化简
1.删除非产生
2.删除不可达
3.删除$\varepsilon$
4.删除单表达式

正则语言的左线性文法和右线性文法，左和右说的就是非终结符是在左边还是右边

乔姆斯基范式  像二叉树
格雷巴赫范式  每次必产生一个终结符

PDA 下推自动机
对应2型语言的是NPDA（非确定下推自动机）
而DPDA能够接受的语言是小于2型语言的
比如说
$$
L=\{w w^R\}
$$
这个就只能靠NPDA来接收，我猜在任何一刻下一个字符就要转入回文

PDA的状态转移
$$ q_i \xrightarrow[]{读入字符，即将出栈的栈顶字符/入栈的字符串} q_j$$
注意，一般认为左边是栈顶，右边是栈底，所以我们应该是这么写的
$$ q_i \xrightarrow[]{a，Z_0/aZ_0} q_j$$
还有就是，对于空栈接收的PDA，我们要将Z_0也弹出去
$$ q_i \xrightarrow[]{\varepsilon，Z_0/\varepsilon} q_j$$

正则语言 上下文无关语言 图灵机的短语结构语言 的 封闭性 和性质（空有穷无穷，成员性，等价）可判定性
正则语言几乎对所有语言的运算封闭
上下文无关语言对除了求补和交运算以外的预算都封闭（注意求补和交运算可以弄出差）
一个上下文无关语言和正则语言取交集得到的结果应该仍然是一个正则语言

可以用上面的性质来给出是或不是正则语言/上下文无关语言的判断，给出了一个除了泵引理以外的方法

性质可判定性 对于正则语言几乎都可以判定，
对于上下文无关语言能判断成员性，能够判断是否为空，是否有限，是否无穷
但是，不能判断等价性，不能判断包含性问题，不能判断是否为满集



泵引理 判断正则语言和上下文无关语言，注意泵引理的实际范围是比它们要大的，所以实际上并不是等价。


对了，还有一个“格局（Configuration）”
用来表示一个状态机更加确切的状态
有限状态机  

$$
\begin{aligned}
&(q_i,abcdw)\vdash(q_j,bcdw)\vdash^*(q_k,w)
\\
&\text{左边q表示状态，右边字符串表示剩下未读入的字符,带星号表示进行了多次步进}
\end{aligned}
$$
下推自动机
$$
\begin{aligned}
&(q_i,abcdw,abZ_0)\vdash(q_j,bcdw,abZ_0)\vdash^*(q_k,w,bZ_0)
\\
&\text{和上面那个同理，第三个元是指目前的栈，左边是栈顶，右边是栈底，记得写}Z_0
\end{aligned}
$$
图灵机
$$
\begin{aligned}
&X_1 X_2 q_1 X_3 X_4 \vdash X_1 X_2 Y q_2 X_4
\\
&\text{这里的X表示字符，q表示的是对应的状态，指针指向的是q右边的符号}
\end{aligned}
$$

然后就是图灵机了，这个感觉它给的题目都没难度

